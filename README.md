# ghostty
ghostty configuration for Arch Linux (EndeavourOS - KDE Plasma - Wayland)

## Installation
```sh
git clone https://github.com/joelhenwang/ghostty.git "${XDG_CONFIG_HOME:-$HOME/.config/}"
```
